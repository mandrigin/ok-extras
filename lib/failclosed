#!/bin/bash
set -euo pipefail
install -d -m 0755 /var/lib/omarchy-kids
printf '1\n' > /var/lib/omarchy-kids/failclosed
chmod 644 /var/lib/omarchy-kids/failclosed
for file in /sys/fs/cgroup/omarchy-kids/*/cgroup.freeze; do
  [[ -f $file ]] || continue
  printf '1\n' > "$file" || true
done
